# CLAUDE.md · claude-agent-skills 接续规约

> **新会话启动锚点。** 在这个目录下开 Claude，先读本文件。
> **读者是 Agent。** 人是维护者、不是读者。
>
> 分工：**`README.md` 给"任何读者"**（含 GitHub 访客）；**本文件只管"在这里干活时"的规矩**。

---

## 一 · 这是什么

**skill 的开发与维护仓库**，不负责加载。

一个 skill 的完整形态 = **`SKILL.md`（意图层）+ `harness/`（可拦的那几层）**。

```
<skill-name>/source-layer/            ← 来源与维护层（过程）
  record记录-*.md                     ← 原始材料：需求、纠偏、测试、决策
  <skill-name>/                       ← 成品层（**只有这层是拿去加载的**）
    SKILL.md · harness/
```

**取用**：`cp -r <skill-name>/source-layer/<skill-name>/ ~/.claude/skills/`

---

## 二 · 三条硬约束（**违反会被门拦**）

| # | 约束 | 谁拦 |
|---|---|---|
| **1** | **私有项一律用 `<…>` 占位符**（私有仓名 / 内部目录名 / 绝对路径 / 邮箱 / 密钥） | `tools/check_public_hygiene.py` —— **本地 pre-commit + CI 双门** |
| **2** | **不许静默跳过检查** —— 清单缺失时脚本**直接报错**；只有显式 `--no-private-list` 才允许跳 | 同上 |
| **3** | **不许直推 `main`** —— 须走 PR + `hygiene-check` 必需检查 | 分支保护（`enforce_admins` 也开） |

**改完必跑这两条**：

```bash
python3 tools/check_public_hygiene.py --self-test   # 证明门还活着（11 项）
python3 tools/check_public_hygiene.py               # 本仓校验
```

> **为什么第 2 条存在**：清单缺失时静默跳过，是**"该有输入的地方没输入、系统不报错、产出看起来正常"** ——
> 本项目点名要防的形状。**跳过必须是"说出来的"。**

---

## 三 · 接续协议（**新会话照这个定位**）

1. 读本文件 → 再读 `README.md`（结构约定的完整版）
2. **看 §五「当前状态」** —— 哪个 skill 到哪一步
3. 改动 → 开分支 → commit → push → **PR** → ⛔ **等人说"可以合"** → 才合

### ⛔ 合并需要一句显式授权

**CI 绿是质量判断，不是授权。**

理由见 `agent-pitfalls-kb` 的 `A7-R1`（对外动作必须能指出一条显式指令）。
**单人仓里「点合并」就是唯一的人工审核位** —— 它不能被自动化掉。

> 本仓 2026-09-15 实测：Agent 自开 PR + 自合 ⇒ **整条链上零次人工确认**，
> 被执行层的授权门拦下。**这就是本仓 `harness/outward-guard.py` 要拦的东西。**

---

## 四 · 怎么更新（**三类改动，各有规矩**）

| 改什么 | 规矩 |
|---|---|
| **加一个新 skill** | 建 `<skill-name>/source-layer/<skill-name>/SKILL.md`（**必须大写**）；需要门禁就带 `harness/` |
| **更新现有 skill** | 直接改 `source-layer/<skill>/`；**若动了判据或门禁，必须同步更新 `harness/README.md`** |
| **改门禁脚本** | 改完**必须**跑 `--self-test`；**判据改一个字，自测样本可能就不再覆盖它** |

### 三条更新纪律

1. **新知识先落 `record记录-*.md`，再改 `SKILL.md`** —— 记录是 SKILL 的依据，不是附属品
2. **凡改门禁，跑 `--self-test`** —— 门会自己腐烂；不跑自测 = 不知道它还活着
3. **自测里的"反证"必须先证明它「能被证伪」** —— 否则它只是另一条永远绿的测试
   > 本仓踩过：一条反证**因为样本没命中触发词**，什么都没测到，却**"通过"了**。
   > 修法是加一行 `assert`，先证明样本**真的命中**。

---

## 五 · 当前状态（**每次改动后请更新本节**）

> ⭐ **要接手，先读 `grilling-agent/source-layer/接手总览-20260915.md`** ——
> 那一份是**入口**：从哪读起 · 现在在哪 · 下一步 · **别踩的坑** · 方法论。
> **本节只是快照，细节都以那份为准。**

| skill | `SKILL.md` | `harness/` | `evals/` | `source-layer/` |
|---|---|---|---|---|
| **`grilling-agent`** | ✅ **149 行 · 五条规则** | ✅ 3 门启用 · 1 门**实测否定** | ✅ 20 条评测集 · **4 个脚本** · 6 条边界样本 | ✅ **11 份记录**（01–11） |

**触发率实测**（2026-09-15 · 详见 `record记录-08`）：

| | 按条 | **按次** |
|---|---|---|
| 该触发时命中 | 10/10 = 100% | 37/50 = **74%** |
| 不该触发时误触 | 1/10 = 10% | 2/50 = **4%** |

⚠️ **这几个数都要打折看** —— 正例**一个字没改**，两轮之间按次命中率从 53% 涨到 74%。
**说明这个测量本身抖动很大。** 只有**跨两轮都稳**的才敢当结论。

**装在哪**：**项目级**。**用户级不装** —— `~/.claude/skills/` 下没有它（已裁决）。

**`harness/` 的四道门**：

| 门 | 仓库内 | **本机装载** |
|---|---|---|
| ① `outward-guard.py` 对外 / 不可逆 | ✅ | ✅ **用户级**（已在真实场景触发过多次） |
| ② `bulk-write-guard.py` 批量改写 | ✅ | ✅ **用户级** |
| ③ `path-guard.py` 路径白名单 | ✅ | 🔶 **应装项目级** —— 白名单**每个项目不同** |
| ④ `stop-guard.py` 意图代理 | ✅ | ⛔ **不装** —— 241 条真实语料上 **100% 误报、真阳 0** |

> **装哪一层的判据**：**通用知识 → 用户级；项目特定知识 → 项目级。**
> （③ 的白名单是每项目不同的 ⇒ 装宽了等于没拦。）
> ⚠️ **③ 的真实触发测试还没做。没测之前，不要说它生效了。**

---

## 六 · 与其他仓的关系

| 仓 | 关系 |
|---|---|
| **`agent-pitfalls-kb`** | **规则库**（给 Agent 读的可执行规则）。本仓的 `harness/` **正是它 `A7` §6 设计的那道「对外动作门」的实现** |
| `agent-eval-gate` | 无关 |

> ⚠️ 本仓**不重复**规则库的内容。**要从规则库里学的，去读规则库；本仓只放"这个 skill 自己的东西"。**

---

## 七 · 已知缺口（**不假装全绿**）

| # | 缺口 | 影响 |
|---|---|---|
| **1** | **B1–B6 六条边界样本，一条都没跑** | 它们才是「适用 / 不适用」两栏的**校准器** |
| **2** | **`SKILL.md` 里 ①②打架时谁压谁，没写** | B5 暴露的**真空白**：规则②说"能核就别问"、规则①说"指不出依据就问"，**在「指代悬空」上直接打架** |
| **3** | **这个 skill 到底加了多少价值 —— 没测** | 本轮只测**触发**、不测**行为**。手上只有**一个轶事**，不是证据 |
| **4** | `desc-opt` 那 5 轮结果**全部作废** | ⛔ **别用它的 `final_description`**。见 `record记录-09` |
| **5** | ③ `path-guard` 还没做真实触发测试；④ 不装 | ③ 在测之前**不要当它生效了** |
| **6** | `tools/private-names.txt` **只在本机**（被 `.gitignore` 排除） | **换机器要重建**，否则"私有名"那项在本地也是空的 |
| **7** | `core.hooksPath` **不随 clone 携带** | 换机器要重跑 `git config core.hooksPath .githooks` |
| **8** | **`-workspace/` 里的转录原件不随 clone 走** | 结论已进记录；**但机器没了转录就没了**（要不要脱敏后提交，未决） |
