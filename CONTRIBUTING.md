# CONTRIBUTING

> **先说清楚这是什么地方。**

## 这是一个**个人仓**

它维护的是**一个人**在真实协作里长出来的 skills ——
每个 skill 的依据是**他自己的**：需求、被纠正的记录、实测数据。

**所以：**
- **不收"通用最佳实践"** —— 那些没有来源，进了本仓就是无根之木
- **不收"我觉得应该"** —— 本仓收的每一条都要能指回一次**真实事件**
- **PR 可能不被合** —— 这不是评判你的方案，是本仓的取舍标准本来就窄

## 想参与，三条路

| | 做什么 |
|---|---|
| **1** | **提 issue**（推荐）—— 说清"哪个 skill 的哪条规则，在你那儿出了什么形状的问题"。**有实测数据最好。** |
| **2** | **提 PR** —— 先读 `CLAUDE.md` §四「怎么更新」，那里有三条更新纪律 |
| **3** | **Fork 走** —— 本仓是 MIT，**你完全可以拿去做你自己的**。可能比提 PR 更省事 |

## 提 PR 之前

**读这个顺序**，别跳：

1. `README.md` —— 结构约定（读者是 Agent）
2. **`CLAUDE.md`** —— 工作规约：硬约束 / 更新纪律 / 当前状态
3. 你要改的那个 skill 的 `source-layer/record记录-*.md` —— **先看它凭什么成立**

## 三条最容易踩的

1. **占位符** —— 私有仓名 / 内部目录名 / 绝对路径 / 邮箱 / 密钥，**一律用 `<…>`**（门会拦）
2. **改了门禁就要跑 `--self-test`** —— 判据改一个字，自测样本可能就不再覆盖它
3. **新知识先落 `record记录-*.md`，再改 `SKILL.md`** —— **记录是依据，不是附属品**

## 许可

MIT。见 `LICENSE`。
